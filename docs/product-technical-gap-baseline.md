# Product and technical gap baseline

Last reconciled: 2026-09-12. Product owner: Policy Fact Authoring. Status: pre-release / Proposed foundation, not a deployed compliance product.

## Evidence authority and preservation

This is the active decision and gap ledger. The preceding ledger is retained without byte changes in [the historical evidence ledger](evidence/product-gap-history-through-20260909.md), Git blob `417a57e8f127c51d94ef79589b9feedabfda87b1`. Its earlier open-stack descriptions, dated status observations, run IDs and test totals are historical evidence only. The reorganization preserves every historical receipt and requirement while removing contradictory current-head claims from the active ledger.

Fresh inspection still finds protected `main@52f4fd6bb68f870d0519cf11dd471573a2f197c0` at the bootstrap boundary. The implemented work is integrated on `develop` behind open PR #1, not shipped on protected main. Historical PR #13 is merged; it is not a second open writer. Issue #12 remains open because protected integration, independent review and dependency license/distribution acceptance are not established merely by merging the former side branch.

A check receipt must bind repository, PR, source head, base, actual checkout SHA, run/attempt/job and artifact identity. An associated PR run that checks out a synthetic merge commit is integration evidence, not automatically a literal head checkout. Re-fetch final candidate evidence after every head or base movement. No queued, skipped, cancelled, predecessor or comment-only result constitutes approval or passing required Checks. Ready is review admission, not merge authorization.

## Runtime status admission repair

[ADR-0004](ADR-0004-runtime-status-admission.md) implements the existing PRD/TRD/ADR-0002 explicit-fact requirement in `src/policy.ts`. Review, completed-step derivation and schema-v1 export no longer treat truthy unknown collection/retention/transfer status values as operator confirmations. Only exact existing members are accepted; unsupported statuses retain their owning finding and export as `null`, without coercion, inferred `no`/`none` or source mutation.

Test-only `557a58cd34d031c0cadb17dabdf101f2a4819198` produced hosted RED in CI `34682157436`, job `103522654450`: 44 failed / 119 passed across 163 Vitest cases. All failures were the intended new categorical-admission assertions. The actual checkout was merge commit `239b0f7f161606d5de1b3050a3be0ba4d65a2554`, against protected base `52f4fd6bb68f870d0519cf11dd471573a2f197c0`.

Production repair `23ceef47a4dacc9365b426c7cab8b9b671626768` has policy blob `9a16f2614de0cb4b1970e72c147e7693f76f88f2`. Strict local TypeScript compilation and the identical-assertion Node test adapter pass all 81 status cases; this does not claim a local full Vitest/browser/PostgreSQL run. Its hosted generation was cancelled after concurrent `develop` advances. The concurrent local-preview/README/security-navigation work through `887fb8681f2588e21f5ea86de08d295dc3f96cb4` is preserved, including its [bounded doctoring record](doctoring/local-preview-readme-boundary.md). Final integrated hosted evidence remains a live PR merge gate, not a self-referential claim embedded in this file.

## Action-runtime cleanup — historical receipts

These two immutable receipts are retained for the existing source contract; they do not verify the current candidate. Historical head `23b3a1c7429aca2ea39e153c6504beee9d507d39` CI `34179633214` produced the action-runtime evidence; artifact `10038466559` is bound by digest `sha256:83d8e177c78856a41eb58c3312fb8a671903989deb7b3bb2b8a747f8eea9091b`.

Historical head `fba59b85c5ce33813147939f8124cb2d69b184a1` CI `34180350163` produced the integrated action-runtime evidence; artifact `10038696518` is bound by digest `sha256:f31fd4d0dee1164e76e0841f01cc2c5f6f9ce73c6b07fc08e98ac9bd4b333fa8`.

## Current baseline

| Area | Evidence and status | Commercialization gap | Owner/action | Next verification |
| --- | --- | --- | --- | --- |
| Guided authoring | PRD, TRD, ADR-0002, seven routed editors, truthful first-step startup, review/navigation functions; implemented foundation | Preserve independent collection, retention and transfer authority; collection-path evidence is free text, not a verified source receipt | Policy Fact Authoring: admit only established facts and introduce structured path evidence only for a demonstrated integration | Current-candidate unit/UI and browser cases |
| Runtime categorical admission | ADR-0004, three private predicates and 81-case status matrix; source repaired after hosted RED | A complete external-input parser and import/migration contract do not exist | Policy Fact Authoring: preserve exact vocabulary, null invalid projection and independent finding ownership | Full candidate Vitest/build plus invalid/valid cross-product and non-mutation cases |
| Draft portability | `createPolicyExport`, schema-v1 JSON, fixed-name local Blob download, unsafe URL rejection and allocation-aware success/error cleanup; implemented foundation | Import/version migration, cancellation, localized resources and immutable publication remain absent | Policy Fact Authoring: keep deterministic local projection without lossy URL rewriting or network transfer | Browser payload, repeated bytes, keyboard/touch, preparation/activation error and future import contracts |
| Customer-fact authority | Zero-inferred startup, explicit no-collection, independent retention and explicit transfer states, stale-detail invalidation | Hosted persistence must retain independent authority; unknown runtime values cannot become confirmations | Policy Fact Authoring: preserve valid independent facts and reject contradictions | Owning-step, status, transition and persistence invariant cases |
| Review workspace | Live preview, blocker counts, warning-to-owner navigation, logical heading focus and viewport harness | Coverage is bounded; no completed formal approval/publication flow | Review & Publication / UX: retain transparent next-action guidance | Candidate keyboard, focus-scroll and responsive artifacts |
| Accessibility | Focus contrast >=3:1, muted text >=4.5:1, axe, heading visibility, retention transitions and bounded reflow harness | Native browser UI zoom, screen-reader and manual WCAG evidence remain incomplete | UX/Test Engineering: record real assistive-technology and manual sessions; do not infer conformance from axe | Candidate screenshots and manual interaction records |
| Legal/rule traceability | `research-traceability.md`, PIPA/PIPC source/effective-date model; source baseline | Product completeness is not legal sufficiency; no versioned article/section rule engine | Legal Source Registry: source/effective-date/revision mapping through ACL | Authoritative snapshots and effective-date fixtures |
| Policy model | ADR-0003, ERD, 3NF up/down migration, exact-domain-error, lock scheduling, UPSERT, restart and custom restore scripts; Proposed CI-only database | Tenant authorization, immutable audit, encryption/key handling, deletion and production-scale contention remain unproved | Platform: keep hosted adapter disabled; preserve independent no-collection/retention constraints | Candidate PostgreSQL 18 apply/down/apply, concurrency, restart/restore, then hosted security tests |
| Publication | Planned `publication_revision`; readiness CTA does not pretend to publish | No authenticated approval, immutable publication, supersession, rollback or public-URL lifecycle | Review & Publication: implement only after persistence/security prerequisites | Auth, replay/digest, supersession and lifecycle tests |
| Security/privacy and local evaluation | `docs/SECURITY.md`, memory-only runtime, credential/query/fragment URL rejection, concurrent loopback preview contract | Hosted isolation, audit/incident evidence, encryption and operational recovery remain absent; no disclosure is inferred from a configuration defect | Platform/Security: threat-model any networked deployment before enabling it | Local-listener smoke and current security checks; hosted threat/authorization cases later |
| Dependency supply chain | Exact declarations, lock consistency/scope/license contracts and separate CycloneDX artifact; integrated on develop | Issue #12 still needs protected integration, applicable license/NOTICE/distribution review and organization security evidence | Product owns dependency intent; central CI/security owns scan and release gates | Candidate clean install, artifact digest, inventory, vulnerable/unknown/incompatible component review |
| Test assurance | Existing unit/UI/schema/workflow/dependency/boundary suites plus 81 status cases, PostgreSQL scripts and browser matrix | Repository-wide 100% execution/branch/meaningful-edge coverage is not evidenced by test counts; scoped browser skips remain disclosed | Test Engineering: measure coverage rather than asserting it | Candidate actual runner totals, coverage, warnings and test artifacts |
| Performance/deployment | Static Vite browser product; no hosted product backend | No realistic full operator-flow browser performance or k6 baseline, compose portability or hardware-tuning evidence | Operability: measure real render/network/runtime paths; all pages retain p95 <=20 ms target without sample exclusion or unrealistic warmup | Measured browser baseline, then realistic k6 and Docker/Podman/Colima deployment evidence |
| UX/i18n resources | Korean authoring UI; source-owned styles/components | No complete Figma/Storybook scene matrix or DB-backed versioned ko/en/ja/zh/vi/es/de/fr resource authority | UX/Translation owner: released resource contract with screen-key fetch/cache, review/approval/deploy/rollback; keep ontology labels separate | Locale wrapping/CJK/expansion, permission/loading/error and interaction E2E |
| CI/security merge gate | Ruleset `18156473` still requires one approval, resolved threads and central workflows; no force update or bypass used | Dependency Review returned HTTP 403 at exact test head; CodeQL dispatch acceptance and model reviews require current owner-side evidence; product PR checkout identity must not be mislabeled | `.github` owns canonical workflow/dispatcher repair; preserve independent review and exact source/checkout identity | Terminal current required checks, fresh approval, source/head/base consistency and ordinary protected merge |

## DDD, context map and persistence contract

Core subdomain: **Policy Fact Authoring**. Supporting: **Review & Publication** and **Legal Source Registry**. Generic identity, workflow, provider and observability services do not own product facts. `Policy Fact Authoring -> Review & Publication` supplies a versioned fact contract. `Legal Source Registry -> Review & Publication` supplies source/rule revisions through an ACL. External customer/source-system adapters do not mutate foreign systems through hidden coupling.

Ubiquitous language: `policy_revision`, `service_profile`, explicit no-collection attestation, `retention_status`, `collection_item`, `processing_purpose`, `retention_rule`, `third_party_transfer`, `international_transfer`, `privacy_contact`, `review_finding`, `legal_source_revision`, `publication_revision`. The Proposed minimal aggregate is revision-owned facts under `policy_revision`. Categorical predicates are domain admission rules, not a generic external schema authority or a new aggregate.

Collection and retention remain independent revision-owned facts. `retention_rule` exists only for `retention_status = applies`. When transitioning away from `applies`, delete the current `retention_rule` in the same transaction. A retained `retention_rule` causes the deferred constraint to reject the commit. Immutable audit history remains separate. Preserve revision ownership, tenant-scoped version uniqueness, 3NF, parent-row lock coordination, complete item-key UPSERT/idempotency and NULL-safe restored-value assertions. Rendered prose is a read projection, not an authority store.

Identity, ontology, translation and other ecosystem consumers use released versioned contracts with ACLs only. An immature owner remains an owner-side development target; until release, keep integration behind ports/feature flags/test doubles. No copied supplier source, cross-service SQL, temporary-branch dependency or unapproved publication is admitted.

## Active commercialization order and release gates

First stabilize and verify the current integrated `develop` candidate in PR #1, review actual failed-job logs at the canonical owner and acquire qualifying approval. Continue independent source/test/docs work while any check waits; do not replace absent evidence with a synthetic verdict or retire unmerged valid delta.

Next close measured accessibility/manual-assistive-technology and local-evaluation gaps, then prove hosted tenant authorization, immutable audit, encryption and operational backup/restore before enabling a persistence adapter. Follow with authenticated immutable publication/supersession, released ecosystem contracts, DB-backed locale resources, portable deployment and measured performance. Existing PostgreSQL CI execution is not a hosted release.

A publishable release requires complete fact-to-warning navigation, deterministic rendering, no invented operator facts, explicit independent states, validated source/rule versions, accessible responsive interaction, governed immutable publication, hosted security/operability and current CI/security/review evidence. Public wording describes assistance and reviewability, never a compliance guarantee or an internal implementation boundary. GitHub Pages or an immutable release is not claimed without actual publication evidence.
